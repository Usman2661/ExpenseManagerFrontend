import React from 'react';
import ExpenseList from '../Staff/ExpenseList';
import StatCards from '../Staff/StatCards';

export default function ManagerSeniorExpense() {
  return (
    <div>
      <StatCards /> <ExpenseList />
    </div>
  );
}
